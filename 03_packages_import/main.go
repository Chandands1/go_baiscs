package main

//imports bring external packages into the file that we aare working
//wehre actually we need it
import(
	"fmt"
	"math"
)

func main(){
	// packageName.FunctionName -> call a function from the package

	fmt.Println("sqrt(25)", math.Sqrt(25))
}